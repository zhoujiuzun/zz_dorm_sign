# 宿舍自动签到系统 · 项目说明

> 中南林业科技大学"平安打卡"小程序的自动签到系统。受众=自己+少数熟人。
> 一句话:**熟人一辈子抓一次 openid 入库,服务器每晚自动到点签到,看板查结果。**

## 1. 它解决什么

学校"平安打卡"要求每晚 21:00–22:30 在宿舍上报位置。本系统代你自动完成:
- **openid 永久不变**,每人只需用 PC 微信抓一次入库(见 `capture/`)。
- 阿里云函数计算(FC)每晚定时器到点,用每人 openid 登录学校系统→取签到任务→按各自登记的宿舍坐标提交签到。
- 失败才推送通知给管理员;看板展示全员签到状态与历史。

## 2. 架构总览

```
[采集端 PC]  openid-capture.exe (mitmproxy 抓 openid)
      │ POST /register (带预共享口令)
      ▼
[阿里云 FC 单函数 sign]  python3.10, cn-beijing, 双触发器
      ├─ HTTP 触发器:   /register 注册 · /api/* 看板数据 · 登录/黑名单管理 · 静态页
      ├─ 定时触发器×2:  北京 21:00-21:59 & 22:00-22:20 每分钟,各人按"专属时刻"签到
      │                  通知 → Server酱推管理员
      ▼
[阿里云 OSS 桶 zhouhuanzhang]  私有,FC 凭 RAM 角色走内网读写
      ├─ users.json     名册 {openid,昵称}  含敏感 openid
      ├─ history.json   流水 {昵称,日期,时间,结果}  永久,不含 openid
      └─ blacklist.json 黑名单
      ▲
[本地看板 dashboard.py]  localhost:8765,转发线上 /api/* 渲染看板(绕开 fcapp.run 强制下载)
```

**关键设计**(详见 `docs/adr/`):
- **各自本地抓包**:安卓 7+ 不信任用户证书,微信流量只能在 PC 微信上 MITM。openid 物理上只在登录瞬间的 `getOpenidByJsCode` 响应里出现,只能抓包获取。
- **OSS JSON 存储**:数据量极小,关系库是过度设计;名册/历史分离,状态页只读不含 openid 的历史。
- **永久在线部分极简**:只有"注册接口+定时器"常驻,放 Serverless,几乎零成本。

## 3. 核心流程

**采集(每人一次)**:PC 微信登录小程序 → `openid-capture.exe` 进程内 mitmproxy 截 `getOpenidByJsCode` 响应取 openid → POST `/register` → 后端用 openid 登录学校系统自动取**真名**入册(无效 openid 当场拒)。

**签到(每晚自动)**:定时器每分钟触发 → `run_minute_sign` 挑出"专属时刻=当前分钟"的人 → 每人:`login_via_openid` 换 token → `getListForApp` 取任务 → `getTaskByIdForApp` 拿**各自宿舍坐标** `dormitoryRegisterVO.locationLat/lng` → haversine 实算距离填 `locationAccuracy` → 提交签到 → 结果追加 history。失败汇总推 Server酱。

> ⚠️ 坐标必须取每人的 `dormitoryRegisterVO`(各不相同),旧 `sign(1).py` 用全局写死坐标是 bug,已弃用。

**看板**:`python tools/dashboard.py` → 浏览器开 `127.0.0.1:8765`。访客只读;右上角 admin 登录后可拉黑/恢复/删除成员(拉黑=从名册移入黑名单,定时签到自然跳过)。前端用 `member_id`(openid 的 sha256 前 12 位)定位成员,不暴露 openid。

## 4. 文件清单

### 后端 `sign/`(部署到 FC)
| 文件 | 作用 |
|---|---|
| `index.py` | FC 入口,分发定时签到 / HTTP 请求;`run_minute_sign` 按专属时刻签到 |
| `web.py` | HTTP 路由:注册、看板 API、登录、黑名单管理、静态页 |
| `sign_core.py` | 签到核心:专属时刻计算、学校 API 登录/取任务/提交、DNS 绕过(DoH) |
| `oss_store.py` | OSS 存储层:users/history/blacklist 读写;`member_id` 隐私哈希 |
| `notify.py` | Server酱推送签到异常给管理员 |
| `static/` | 看板前端 index.html / app.js / style.css |
| `requirements.txt` | 依赖(已固定版本) |

### 采集 `capture/`(发给熟人)
| 文件 | 作用 |
|---|---|
| `capture.py` | 采集工具源码:进程内 mitmproxy 抓 openid 上报,自动装/删证书+开/关代理 |
| `dist/openid-capture.exe` | 打包好的成品(22MB,管理员运行) |
| `dist/使用说明.txt` | 给熟人的傻瓜步骤 |
| `BUILD.md` | PyInstaller 打包说明 |

### 本地工具 `tools/`
| 文件 | 作用 |
|---|---|
| `dashboard.py` | 本地看板,转发线上 API 渲染(无需域名/备案) |
| `manage_users.py` | 本地名册增删查(走外网 endpoint,需 AK 环境变量) |
| `_seed_demo.py` | 灌/清演示数据(`--clear`),按 demo openid 前缀识别 |
| `test_sign.py` | 用真 openid 验证签到链路 |

### 部署配置(根目录)
| 文件 | 作用 |
|---|---|
| `deploy.ps1` | 部署脚本(`s deploy`,**含明文密钥,勿外传**) |
| `.env` | 部署实际读取的密钥源(**勿外传**) |
| `s.yaml` | Serverless Devs 部署配置 |

### 文档 `docs/`
`项目说明.md`(本文,在根目录) · `docs/Review报告.md`(代码审查) · `docs/私密信息汇总.md`(密钥索引,勿外传) · `docs/AK-SK凭据.txt`(AK/SK 明文) · `docs/CONTEXT.md`(术语表) · `docs/adr/`(架构决策) · `docs/域名配置步骤.md` · `docs/Cloudflare部署步骤.md`

<!-- APPEND-MARKER -->

## 5. 部署与运维

**部署后端**:在项目根目录 PowerShell 跑 `.\deploy.ps1`(内含设三个密钥 + `s deploy`)。需 Serverless Devs(`s`)已装、部署用户有 `AliyunFCFullAccess`(含 PassRole)。`s.yaml` 已配 `TZ:Asia/Shanghai`(否则容器 UTC 会签错日期)。

**线上地址**:`https://sign-bprgxfyexe.cn-beijing.fcapp.run`(临时域名,无需备案)。
> 该域名强制 `Content-Disposition:attachment`,浏览器直接访问会下载而非渲染看板,故看板走本地 `dashboard.py`。要网页直接打开需绑自定义域名 `sign.zhztool.top`(已购,**未备案**,备案后取消 `s.yaml` 里 `customDomains` 注释 + 配 DNS CNAME)。

**关键运维知识**:
- **FC 解析不了 .edu.cn**:容器内 DNS 解不出 `simp.csuft.edu.cn`。`sign_core.py` 加载时 monkeypatch `socket.getaddrinfo`,拦截学校域名改用 DoH(223.5.5.5)取 IP,写死 `218.76.12.57` 兜底(可用 `SCHOOL_IP` 覆盖)。
- **RAM 角色信任策略**须含 `"Service":["fc.aliyuncs.com"]`(曾误设 RAM root 导致 assume 失败)。
- **管理本地名册**:`manage_users.py`(需设 `ALIBABA_CLOUD_ACCESS_KEY_ID/_SECRET`)。
- 所有敏感值见 `私密信息汇总.md`。

## 6. 上线状态(截至 2026-06-17)

- ✅ 全链路已验证:签名算法、坐标获取、坐标修正、凭据注入、OSS 读写、口令校验、隐私剥离、时区均正确。
- ✅ 后端已部署 FC 并运行;采集 exe 已打包验证(进程内 mitmproxy 自检通过)。
- ⏳ **仍待实测**:21:00 后真实签到确认 `status=ok`(凌晨跑会是 `not_yet` 属正常)。
- ⏳ 多次定时触发会重签,学校对"已签到"返回的 msg 未知,可能被误判成 error 触发假失败通知,需实测调整。

## 7. 已知问题(详见 Review报告.md)

| 优先级 | 问题 | 状态 |
|---|---|---|
| 🔴 | 管理员弱口令默认值 + 令牌可伪造 | 补丁就绪,待重部署 |
| 🔴 | OSS 读失败会回写覆盖,一次抖动可清空历史 | 补丁就绪,待重部署 |
| 🔴 | 定时签到循环内大 sleep,多人同分钟可能 FC 超时漏签 | 补丁就绪,待重部署 |
| 🟠 | 历史按昵称聚合,真名重名会串档 | 需数据迁移,中期做 |
| 🟢 | 演示数据误删、dashboard 导入、依赖版本 | ✅ 已修复 |

**安全提醒**:`SERVERCHAN_KEY` 曾暴露建议重置;AK/SK 明文裸存建议重置;`REGISTER_SECRET` 已随 exe 分发视为半公开,务必靠服务端 openid 校验防滥用。

